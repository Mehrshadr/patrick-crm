# راهنمای کامل DNS و Cloudflare

> این داکیومنت برای یادگیری مفاهیم DNS، انواع رکوردها، و نقش Cloudflare نوشته شده.

---

## 📖 فهرست مطالب

1. [DNS چیست؟](#dns-چیست)
2. [چرا DNS مهمه؟](#چرا-dns-مهمه)
3. [انواع DNS Records](#انواع-dns-records)
4. [Nameserver چیست؟](#nameserver-چیست)
5. [TTL چیست؟](#ttl-چیست)
6. [Cloudflare چیست و چه نقشی داره؟](#cloudflare-چیست)
7. [پروسه انتقال دامنه](#پروسه-انتقال-دامنه)
8. [چک‌لیست انتقال](#چک‌لیست-انتقال)

---

## DNS چیست؟

**DNS = Domain Name System** (سیستم نام دامنه)

DNS مثل **دفترچه تلفن اینترنت** هست. وقتی تو آدرس `google.com` رو تایپ میکنی، کامپیوترت نمیفهمه این یعنی چی. کامپیوترها فقط با **آدرس IP** کار میکنن (مثل `142.250.80.46`).

### 🔄 چطور کار میکنه:

```
تو تایپ میکنی: google.com
         ↓
DNS Server سوال میپرسه: "google.com کجاست؟"
         ↓
جواب میاد: "IP = 142.250.80.46"
         ↓
Browser میره به اون IP
         ↓
صفحه Google باز میشه ✅
```

### 🎯 مثال ساده:
فکر کن میخوای زنگ بزنی به "رستوران محبوبت". تو اسمشو بلدی، ولی گوشیت فقط با شماره کار میکنه. پس میری دفترچه تلفن، اسم رو پیدا میکنی، شماره رو میگیری. DNS هم همینه!

| چیزی که تو میدونی | چیزی که کامپیوتر میخواد |
|-------------------|------------------------|
| `google.com` | `142.250.80.46` |
| `quickfitparts.com` | `192.95.19.213` |

---

## چرا DNS مهمه؟

1. **بدون DNS**: باید IP ها رو حفظ کنی! (غیرممکنه)
2. **با DNS**: فقط اسم دامنه رو یادت میمونه

### 🔧 کاربردهای عملی:
- **وبسایت**: وقتی کسی `quickfitparts.com` میزنه، به سرور درست میره
- **ایمیل**: وقتی ایمیل میفرستی به `info@quickfitparts.com`، ایمیل میدونه کجا بره
- **امنیت**: میتونی ثابت کنی این دامنه مال توئه (مثلاً برای Google Search Console)

---

## انواع DNS Records

هر دامنه چندین "رکورد" داره. هر رکورد یه کار خاص انجام میده:

### 🅰️ A Record (Address Record)

**کار:** دامنه رو به IP وصل میکنه

```
quickfitparts.com  →  192.95.19.213
```

**کی استفاده میشه:**
- وقتی کسی `quickfitparts.com` میزنه → میره به IP سرور

**مثال واقعی:**
| Name | Type | Value |
|------|------|-------|
| `@` | A | `192.95.19.213` |
| `www` | A | `192.95.19.213` |

> **نکته:** `@` یعنی خود دامنه اصلی (بدون www)

---

### 📧 MX Record (Mail Exchange)

**کار:** مشخص میکنه ایمیل‌ها کجا برن

```
ایمیل به info@quickfitparts.com → میره به mail.quickfitparts.com
```

**مثال:**
| Name | Type | Priority | Value |
|------|------|----------|-------|
| `@` | MX | 0 | `mail.quickfitparts.com` |

**Priority چیه؟**
عدد کوچکتر = اولویت بالاتر. اگه چند MX داشته باشی، اول به اولویت پایین‌تر میره.

---

### 🔗 CNAME Record (Canonical Name)

**کار:** یه دامنه رو به دامنه دیگه وصل میکنه (Alias)

```
www.quickfitparts.com  →  quickfitparts.com
```

**کی استفاده میشه:**
- وقتی میخوای `www` به همون جای `@` بره
- وقتی از سرویس‌های third-party استفاده میکنی

**مثال:**
| Name | Type | Value |
|------|------|-------|
| `www` | CNAME | `quickfitparts.com` |
| `blog` | CNAME | `mysite.wordpress.com` |

---

### 📝 TXT Record (Text Record)

**کار:** متن ذخیره میکنه - معمولاً برای verification و امنیت

**کاربردها:**
1. **SPF**: ثابت میکنه ایمیل‌های تو spam نیست
2. **DKIM**: امضای دیجیتال ایمیل
3. **Google Verification**: ثابت میکنه دامنه مال توئه
4. **Search Console**: اضافه کردن سایت به Google

**مثال SPF:**
```
v=spf1 ip4:192.95.19.213 include:spf.web-dns1.com ~all
```

**مثال Google Verification:**
```
google-site-verification=abc123xyz789...
```

---

### 🖥️ NS Record (Nameserver)

**کار:** مشخص میکنه کی مسئول DNS این دامنه هست

```
quickfitparts.com  →  managed by → cloud1.whc.ca, cloud2.whc.ca
```

**این همون چیزیه که ما داریم عوض میکنیم!**

| قبل از انتقال | بعد از انتقال |
|--------------|--------------|
| `cloud1.whc.ca` | `anna.ns.cloudflare.com` |
| `cloud2.whc.ca` | `bob.ns.cloudflare.com` |

---

### 🔐 سایر رکوردها (برای آینده)

| Type | کاربرد |
|------|--------|
| **AAAA** | مثل A ولی برای IPv6 |
| **SRV** | برای سرویس‌های خاص (مثل VoIP) |
| **CAA** | مشخص میکنه کی میتونه SSL صادر کنه |
| **DKIM** | امضای ایمیل |
| **DMARC** | سیاست امنیت ایمیل |

---

## Nameserver چیست؟

**Nameserver = سرور DNS**

Nameserver جایی هست که DNS Records ذخیره میشه. وقتی کسی دامنه‌ات رو میزنه:

```
Browser: "quickfitparts.com کجاست؟"
    ↓
Root DNS: "برو از ns.cloudflare.com بپرس"
    ↓
Cloudflare NS: "IP = 192.95.19.213"
    ↓
Browser میره به سرور
```

### 🔄 انتقال Nameserver یعنی چی؟

یعنی داری میگی: "از این به بعد، Cloudflare مسئول DNS من هست، نه WHC"

**قبل:**
```
دامنه → WHC Nameservers → DNS Records در WHC
```

**بعد:**
```
دامنه → Cloudflare Nameservers → DNS Records در Cloudflare
```

---

## TTL چیست؟

**TTL = Time To Live** (مدت زنده بودن)

TTL به سرورهای DNS میگه: "این اطلاعات رو چند ثانیه cache کن"

### 📊 مثال:

```
quickfitparts.com  A  192.95.19.213  TTL=14400
```

یعنی: "این IP رو ۱۴۴۰۰ ثانیه (۴ ساعت) یادت نگه دار"

### 🕐 چرا مهمه؟

**TTL بالا (مثلاً ۲۴ ساعت):**
- ✅ سرعت بیشتر (کمتر query میزنه)
- ❌ تغییرات دیرتر اعمال میشه

**TTL پایین (مثلاً ۵ دقیقه):**
- ✅ تغییرات سریع اعمال میشه
- ❌ load بیشتر روی DNS Server

### 🎯 استراتژی انتقال:

1. **قبل از انتقال**: TTL رو کم کن (مثلاً ۳۰۰ ثانیه)
2. **منتظر بمون**: به اندازه TTL قبلی
3. **انتقال کن**: حالا تغییرات سریع میشه
4. **بعد از انتقال**: TTL رو برگردون به حالت عادی

---

## Cloudflare چیست؟

Cloudflare یه سرویس هست که چند کار انجام میده:

### 1️⃣ DNS Management

مدیریت DNS با UI راحت:
- اضافه/حذف/ویرایش رکوردها
- تغییرات سریع (معمولاً زیر ۵ دقیقه)
- رایگان!

### 2️⃣ CDN (Content Delivery Network)

محتوای سایتت رو cache میکنه در سرورهای سراسر دنیا:
- سرعت بیشتر برای کاربرها
- load کمتر روی سرور تو

### 3️⃣ Security

- **DDoS Protection**: جلوی حملات رو میگیره
- **SSL/TLS**: HTTPS رایگان
- **WAF**: فایروال برای وب

### 4️⃣ Analytics

- ترافیک سایت
- کشورها
- تهدیدات بلاک شده

---

## چرا داریم دامنه رو به Cloudflare میبریم؟

### دلایل این پروژه:

1. **Google Search Console**
   - برای verify کردن سایت، باید TXT Record اضافه کنیم
   - با Cloudflare سریع‌تر و راحت‌تره

2. **Google Analytics**
   - اگه بعداً نیاز به تغییرات DNS شد

3. **کنترل بیشتر**
   - شما کنترل کامل DNS رو دارید
   - نیازی به درخواست از مشتری نیست

4. **امنیت بهتر**
   - Cloudflare SSL رایگان میده
   - DDoS protection

---

## پروسه انتقال دامنه

### 📊 دیاگرام کلی:

```
┌─────────────────────────────────────────────────────────────┐
│                     قبل از انتقال                            │
├─────────────────────────────────────────────────────────────┤
│  کاربر → quickfitparts.com                                  │
│      ↓                                                       │
│  DNS Query → WHC Nameservers (cloud1.whc.ca)               │
│      ↓                                                       │
│  Response: IP = 192.95.19.213                               │
│      ↓                                                       │
│  کاربر → WHC Server                                         │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                     بعد از انتقال                            │
├─────────────────────────────────────────────────────────────┤
│  کاربر → quickfitparts.com                                  │
│      ↓                                                       │
│  DNS Query → Cloudflare Nameservers                         │
│      ↓                                                       │
│  Response: IP = 192.95.19.213  (همون IP!)                  │
│      ↓                                                       │
│  کاربر → WHC Server  (همون سرور!)                          │
└─────────────────────────────────────────────────────────────┘
```

**نکته کلیدی:** سرور عوض نمیشه! فقط "کی جواب DNS میده" عوض میشه.

---

## چک‌لیست انتقال

### ✅ قبل از شروع:
- [ ] دسترسی به پنل هاست فعلی (WHC)
- [ ] دسترسی به Cloudflare
- [ ] لیست تمام DNS Records فعلی
- [ ] تایید مشتری

### ✅ مرحله ۱: آماده‌سازی Cloudflare
- [ ] Login به Cloudflare
- [ ] Add Site → وارد کردن دامنه
- [ ] انتخاب پلن (Free کافیه)
- [ ] چک کردن DNS Records که Cloudflare اتوماتیک detect کرده
- [ ] اضافه کردن رکوردهای missing (مخصوصاً MX و TXT)
- [ ] یادداشت Nameservers جدید

### ✅ مرحله ۲: تغییر Nameservers
- [ ] Login به WHC
- [ ] رفتن به بخش Domains
- [ ] انتخاب دامنه
- [ ] تغییر Nameservers به Cloudflare
- [ ] ذخیره

### ✅ مرحله ۳: Verification
- [ ] چک کردن Cloudflare (باید سبز بشه)
- [ ] تست سایت: `curl -I https://quickfitparts.com`
- [ ] تست ایمیل (ارسال و دریافت)
- [ ] چک DNS: `dig quickfitparts.com NS`

### ✅ بعد از انتقال:
- [ ] اضافه کردن TXT Record برای Google Search Console
- [ ] Verify کردن سایت در Search Console
- [ ] Setup کردن Google Analytics (اگه نیاز بود)

---

## 🎓 نکات مهم

### ⚠️ اشتباهات رایج:

1. **تغییر Nameserver قبل از آماده‌سازی Cloudflare**
   - نتیجه: سایت دان میشه!

2. **فراموش کردن MX Records**
   - نتیجه: ایمیل‌ها گم میشن!

3. **کپی نکردن TXT Records (SPF)**
   - نتیجه: ایمیل‌ها spam میشن!

### ✅ Best Practices:

1. **همیشه اول Cloudflare رو آماده کن، بعد Nameserver عوض کن**
2. **قبل از انتقال، screenshot بگیر از DNS Records فعلی**
3. **انتقال رو آخر شب یا آخر هفته انجام بده**
4. **بعد از انتقال، همه چی رو تست کن**

---

## 📚 منابع بیشتر

- [Cloudflare Learning Center](https://www.cloudflare.com/learning/)
- [What is DNS? (Cloudflare)](https://www.cloudflare.com/learning/dns/what-is-dns/)
- [Google Search Console Help](https://support.google.com/webmasters/)

---

---

## Proxy چیه؟ (ابر نارنجی vs خاکستری)

### 🔶 ابر نارنجی (Proxy ON):

```
کاربر → Cloudflare → سرور شما
```

**Cloudflare وسطه.** یعنی:
- کاربر IP سرور واقعی رو نمیبینه
- Cloudflare محافظت میکنه
- **فقط برای وبسایت کار میکنه** (HTTP/HTTPS)

### 🔘 ابر خاکستری (Proxy OFF / DNS only):

```
کاربر → مستقیم سرور شما
```

**Cloudflare وسط نیست.** فقط آدرس رو میده، بعد کنار میره.

### 📧 چرا ایمیل باید خاکستری باشه؟

Cloudflare **فقط** ترافیک وبسایت (HTTP) رو میفهمه.
ایمیل یه زبان دیگه حرف میزنه (SMTP).

| حالت | نتیجه |
|------|-------|
| نارنجی | ایمیل → Cloudflare → ؟؟؟ → **گم میشه** ❌ |
| خاکستری | ایمیل → مستقیم سرور → **میرسه** ✅ |

### 🎯 خلاصه یه جمله‌ای:

> **خاکستری = مثل قبل، مستقیم به سرور**
> **نارنجی = Cloudflare وسط میشینه**

---

## پروژه QuickFitParts: چیکار کردیم؟

### 📋 دامنه‌های منتقل شده:
- `quickfitparts.ca`
- `quickfitparts.com`

### 🗑️ رکوردهایی که حذف کردیم:

| رکورد | چرا حذف شد؟ |
|-------|-------------|
| **cpanel** | پنل مدیریت هاست - از IP مستقیم قابل دسترسیه |
| **whm** | پنل ادمین هاست - نیازی به ساب‌دامین نداره |
| **cpcalendars** | سرویس داخلی cPanel - کسی استفاده نمیکنه |
| **cpcontacts** | سرویس داخلی cPanel - کسی استفاده نمیکنه |
| **webdisk** | WebDAV برای آپلود - معمولاً استفاده نمیشه |
| **ftp** | FTP از IP مستقیم کار میکنه - امنیت بهتره نباشه |
| **NS Records** | Cloudflare خودش مدیریت میکنه |

### ✅ رکوردهایی که نگه داشتیم:

| رکورد | چرا نگه داشتیم؟ | Proxy |
|-------|-----------------|-------|
| **A @** | سایت اصلی | 🟠 نارنجی |
| **CNAME www** | آدرس با www | 🟠 نارنجی |
| **CNAME mail** | مسیر سرور ایمیل | 🔘 خاکستری |
| **A webmail** | رابط کاربری ایمیل | 🔘 خاکستری |
| **MX** | کجا ایمیل‌ها برن | DNS only |
| **TXT SPF** | امنیت ایمیل | DNS only |
| **TXT DKIM** | امضای ایمیل | DNS only |
| **TXT DMARC** | سیاست ایمیل | DNS only |

### 🎯 قانون کلی حذف:

> **هر چیزی که لازم نیست → پاک کن!**

دلایل:
1. **امنیت بهتر** - کمتر نقطه حمله داری
2. **مدیریت ساده‌تر** - کمتر گیج میشی
3. **سرعت بیشتر** - DNS lookup سریع‌تره

---

## 🖥️ ساب‌دامین‌های cPanel - هر کدوم چیه؟

وقتی یه هاست از cPanel استفاده میکنه، یه سری ساب‌دامین اتوماتیک ساخته میشه:

### 📁 FTP (File Transfer Protocol)

**چیه:** پروتکل انتقال فایل

**کاربرد:** آپلود/دانلود فایل به سرور

**چطور کار میکنه:**
```
کامپیوتر تو ←→ FTP ←→ سرور
```

**چرا پاکش کردیم:**
- از **IP مستقیم** کار میکنه (نیازی به ساب‌دامین نداره)
- امنیت بهتره که expose نشه
- میشه با `ftp://192.95.19.213` وصل شد

**نکته:** FTP توی Browser نمیاد! نیاز به **FTP Client** مثل FileZilla داره.

---

### ⚙️ WHM (Web Host Manager)

**چیه:** پنل مدیریت سرور برای ادمین‌های هاست

**کاربرد:** 
- ساختن اکانت‌های cPanel جدید
- مدیریت کل سرور
- تنظیمات پیشرفته

**فرق با cPanel:**
| WHM | cPanel |
|-----|--------|
| برای ادمین سرور | برای صاحب سایت |
| مدیریت همه اکانت‌ها | فقط اکانت خودت |
| دسترسی root | دسترسی محدود |

**چرا پاکش کردیم:** از IP مستقیم قابل دسترسیه: `https://IP:2087`

---

### 🎛️ cPanel

**چیه:** پنل مدیریت هاست برای صاحب سایت

**کاربرد:**
- مدیریت فایل‌ها
- ساختن ایمیل
- مدیریت دیتابیس
- نصب وردپرس

**چرا پاکش کردیم:** از IP مستقیم قابل دسترسیه: `https://IP:2083`

---

### 📧 Webmail

**چیه:** رابط کاربری وب برای چک کردن ایمیل

**کاربرد:** مثل Gmail ولی برای ایمیل خودت
- چک کردن inbox
- ارسال ایمیل
- مدیریت فولدرها

**چرا نگهش داشتیم:** کاربرها ازش استفاده میکنن!

**چرا خاکستری؟** چون ایمیل پروتکل جداست (IMAP/SMTP)

---

### 📅 cpcalendars & cpcontacts

**چیه:** سرویس‌های تقویم و لیست مخاطبین داخلی cPanel

**کاربرد:** sync کردن با موبایل (CalDAV/CardDAV)

**چرا پاکش کردیم:**
- ۹۹٪ کسی ازش استفاده نمیکنه
- معمولاً از Google Calendar استفاده میشه

---

### 💾 Webdisk

**چیه:** WebDAV - روش آپلود فایل از طریق وب

**کاربرد:** مثل Google Drive ولی روی سرور خودت

**چرا پاکش کردیم:**
- معمولاً استفاده نمیشه
- FTP یا File Manager رایج‌تره

---

### 🎯 خلاصه:

| ساب‌دامین | نگه داریم؟ | دلیل |
|-----------|-----------|------|
| **ftp** | ❌ | IP کار میکنه |
| **whm** | ❌ | IP کار میکنه |
| **cpanel** | ❌ | IP کار میکنه |
| **webmail** | ✅ | کاربرها لازمشون دارن |
| **mail** | ✅ | ایمیل لازمشون داره |
| **cpcalendars** | ❌ | کسی استفاده نمیکنه |
| **cpcontacts** | ❌ | کسی استفاده نمیکنه |
| **webdisk** | ❌ | FTP رایج‌تره |

---

## مراحلی که طی کردیم:

### مرحله ۱: چک وضعیت فعلی
```bash
dig quickfitparts.com ANY +noall +answer
dig quickfitparts.ca ANY +noall +answer
```
**نتیجه:** TTL = 14400 (۴ ساعت)

### مرحله ۲: Add Site در Cloudflare
1. Login به Cloudflare
2. Add Site → وارد کردن دامنه
3. انتخاب پلن Free

### مرحله ۳: بررسی DNS Records
1. Cloudflare اتوماتیک رکوردها رو detect کرد
2. رکوردهای اضافی رو حذف کردیم
3. mail و webmail رو DNS only کردیم

### مرحله ۴: تغییر Nameservers (در WHC)
از:
```
cloud1.whc.ca
cloud2.whc.ca
```
به:
```
[Cloudflare Nameservers]
```

### مرحله ۵: Verification
- صبر کردیم تا Cloudflare سبز بشه
- تست سایت و ایمیل

---

> 📅 تاریخ نوشتن: ۲۰۲۵-۱۲-۳۱
> 
> این داکیومنت برای پروژه QuickFitParts نوشته شده و میتونه برای پروژه‌های مشابه هم استفاده بشه.

---

## 🎯 راه‌اندازی Google Analytics و Tag Manager

### اطلاعات پروژه QuickFitParts:

| سرویس | ID |
|-------|------|
| **GTM Container** | `GTM-TMGV5W9F` |
| **GA4 Measurement** | `G-4HEFDJX2DX` |

### مراحل راه‌اندازی GTM:

1. **ساخت Account در [tagmanager.google.com](https://tagmanager.google.com)**
   - Account Name: `Quick Fit Parts`
   - Container Name: `quickfitparts.com`
   - Target Platform: `Web`

2. **ساخت GA4 Tag:**
   - Tags → New → Google Tag
   - Tag ID: `G-4HEFDJX2DX`
   - Trigger: `All Pages`
   - Save → Submit → Publish

3. **نصب GTM روی سایت:**
   - از پلاگین Mehrana استفاده شد (v1.8.0+)
   - Settings → Mehrana App → GTM ID

---

## ⚠️ حل مشکل CSP (Content Security Policy)

### مشکل:
سایت یه CSP سختگیرانه داشت:
```
Content-Security-Policy: default-src 'self'
```
این باعث block شدن GTM و GA4 میشد.

### راه حل: Cloudflare Transform Rules

1. برو Cloudflare → `quickfitparts.com`
2. **Rules** → **Transform Rules**
3. Create rule: `Remove HTTP header from response`
4. Header name: `Content-Security-Policy`
5. Deploy

> ⚡ این rule باعث میشه CSP header از response حذف بشه و tracking کار کنه.

---

## 📱 پشتیبانی Microsoft Clarity

### نسخه پلاگین: v1.9.0

فیلد جدید **Custom Head Code** اضافه شد که میتونی:
- کد Microsoft Clarity رو paste کنی
- هر tracking code دیگه‌ای رو اضافه کنی (Facebook Pixel, etc.)

### نحوه استفاده:
1. Settings → Mehrana App
2. فیلد **Custom Head Code**
3. کد Clarity رو paste کن
4. Save Changes

---

## 🔄 خلاصه نسخه‌های پلاگین

| نسخه | تاریخ | تغییرات |
|------|-------|---------|
| 1.7.6 | قبلی | Link Building, Image Optimization |
| 1.8.0 | 2025-12-31 | ✅ GTM Support |
| 1.9.0 | 2025-12-31 | ✅ Custom Head Code (Clarity) |
